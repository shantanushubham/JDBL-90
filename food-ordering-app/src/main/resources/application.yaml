spring:
  application:
    name: Food Ordering App

  datasource:
    url: jdbc:postgresql://localhost:5433/food_app
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      idle-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      time-to-live: 5m

    cache:
      type: redis

jwt:
  secret: SflKxwRJSMeKKF2QT4fwpMeJf513POk6yJV_adQssw5c
  expiration-ms: 86400000

logging:
  level:
    root: INFO