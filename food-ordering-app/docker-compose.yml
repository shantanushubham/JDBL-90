services:

  postgres:
    image: postgres:18
    container_name: food_app-postgres
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: food_app
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: